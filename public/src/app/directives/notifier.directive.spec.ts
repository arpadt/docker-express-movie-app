import { NotifierDirective } from './notifier.directive';

describe('NotifierDirective', () => {
  it('should create an instance', () => {
    const directive = new NotifierDirective();
    expect(directive).toBeTruthy();
  });
});
